# ACE マルチ試験化 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans (inline・推奨：リファクタは相互依存が強い) または superpowers:subagent-driven-development. Steps use checkbox (`- [ ]`).

**Goal:** 既存の単一試験サイト（GenAI Leader・80問）を `exam → category → question` のマルチ試験構造へリファクタし、2つ目の試験 ACE をカテゴリ枠（問題は空でも可）として追加。クイズUIは試験非依存で再利用。

**Architecture:** `exams.ts` を試験メタの正典にし、`schema.ts` の固定enumを廃止して `examId`＋試験別カテゴリ集合で検証。問題JSONは `questions/<examId>/<category>.json` に分離。ルーティングは `/[exam]/categories/[slug]/` の対称プレフィックス。旧 `/categories/[slug]/` はメタリフレッシュで退避。

**Tech Stack:** Astro 6 / TypeScript / Zod / Vitest / GitHub Pages。

**設計の正典:** `docs/2026-06-05-ACE統合-マルチ試験化-設計.md`

> **注:** 本計画は **リファクタ＋ACE枠追加** まで（Task 1〜8）。ACEの**作問**は Task 9（次フェーズ・サブエージェント）。各タスク末でコミット。

---

## Task 1: `exams.ts`（試験メタの正典）＋テスト

**Files:**
- Create: `src/data/exams.ts`, `src/data/exams.test.ts`

- [ ] **Step 1: `src/data/exams.test.ts`（失敗するテスト）**

```ts
import { describe, it, expect } from 'vitest';
import { EXAMS, getExam, getCategory } from './exams';

describe('EXAMS', () => {
  it('has genai-leader (4 cats) and ace (5 cats)', () => {
    expect(getExam('genai-leader')?.categories.length).toBe(4);
    expect(getExam('ace')?.categories.length).toBe(5);
  });
  it('each exam weights sum to 100', () => {
    for (const e of EXAMS) {
      expect(e.categories.reduce((s, c) => s + c.weightPercent, 0)).toBe(100);
    }
  });
  it('category slugs are unique within an exam and intros non-empty', () => {
    for (const e of EXAMS) {
      const slugs = e.categories.map((c) => c.slug);
      expect(new Set(slugs).size).toBe(slugs.length);
      for (const c of e.categories) expect(c.intro.length).toBeGreaterThan(20);
    }
  });
  it('getCategory resolves by exam+slug', () => {
    expect(getCategory('genai-leader', 'fundamentals')?.titleJa).toBeTruthy();
    expect(getCategory('ace', 'setup-environment')?.titleJa).toBeTruthy();
    expect(getCategory('ace', 'nope')).toBeUndefined();
  });
});
```

- [ ] **Step 2: 失敗を確認** — `npx vitest run src/data/exams.test.ts` → FAIL（未実装）

- [ ] **Step 3: `src/data/exams.ts` を実装**（genai-leader の4カテゴリは既存 `categories.ts` の `titleJa/titleEn/weightPercent/intro` をそのまま移植。ace の intro は暫定で、Task 9 の作問時に拡充）

```ts
// src/data/exams.ts
export interface ExamCategory {
  slug: string;
  titleJa: string;
  titleEn: string;
  weightPercent: number; // 各試験内で合計100
  intro: string;
}
export interface Exam {
  id: string;
  slug: string;        // URLプレフィックス
  titleJa: string;     // 正式名
  shortJa: string;     // 短縮名（カード等）
  tagline: string;     // 一言
  description: string;  // 試験トップのSEO説明
  categories: ExamCategory[];
}

const GENAI_LEADER: Exam = {
  id: 'genai-leader',
  slug: 'genai-leader',
  titleJa: 'Generative AI Leader',
  shortJa: 'GenAI Leader',
  tagline: '生成AIの基礎から Google Cloud 製品・活用戦略まで',
  description:
    'Google Cloud「Generative AI Leader」認定試験の無料・日本語練習問題集。公式4分野を即時フィードバックで演習できます。',
  categories: [
    { slug: 'fundamentals', titleJa: '生成AIの基礎', titleEn: 'Fundamentals of gen AI', weightPercent: 30,
      intro: '生成AIの基礎では、大規模言語モデル(LLM)や基盤モデル、トークン、埋め込みといった中核概念と、生成AIが従来の機械学習と何が違うのかを問います。この分野は試験全体の約30%を占め、用語の正確な理解が得点の土台になります。' },
    { slug: 'google-cloud-offerings', titleJa: 'Google Cloud の生成AI製品', titleEn: "Google Cloud's gen AI offerings", weightPercent: 35,
      intro: 'この分野では、Vertex AI や Gemini、Model Garden、Agent Builder など、Google Cloud が提供する生成AI関連サービスの役割と使い分けを問います。試験全体の約35%と最大の比重を持ち、各製品が「何を解決するのか」を結びつけて覚えることが重要です。' },
    { slug: 'improve-output', titleJa: '生成AI出力を改善する手法', titleEn: 'Techniques to improve gen AI model output', weightPercent: 20,
      intro: 'プロンプトエンジニアリング、グラウンディング、RAG(検索拡張生成)、ファインチューニングなど、LLMの限界(ハルシネーション等)を補い出力品質を高める手法を問う分野です。試験全体の約20%を占めます。' },
    { slug: 'business-strategy', titleJa: '成功する生成AIソリューションのビジネス戦略', titleEn: 'Business strategies for a successful gen AI solution', weightPercent: 15,
      intro: '責任あるAI、セキュリティ、ガバナンス、コストや導入推進など、生成AIをビジネスで安全かつ効果的に活用するためのGoogle推奨プラクティスを問う分野です。試験全体の約15%を占めます。' },
  ],
};

const ACE: Exam = {
  id: 'ace',
  slug: 'ace',
  titleJa: 'Associate Cloud Engineer',
  shortJa: 'ACE',
  tagline: 'Google Cloud の構築・デプロイ・運用・セキュリティ',
  description:
    'Google Cloud「Associate Cloud Engineer (ACE)」認定試験の無料・日本語練習問題集。公式5分野を即時フィードバックで演習できます。',
  categories: [
    { slug: 'setup-environment', titleJa: 'クラウド環境のセットアップ', titleEn: 'Setting up a cloud solution environment', weightPercent: 17.5,
      intro: 'プロジェクトや課金、IAMの初期設定、Cloud SDK/gcloud CLI の準備など、Google Cloud を使い始めるための環境構築を問う分野です。（解説は作問時に拡充）' },
    { slug: 'plan-configure', titleJa: 'クラウドソリューションの計画と構成', titleEn: 'Planning and configuring a cloud solution', weightPercent: 17.5,
      intro: 'コンピューティング・ストレージ・ネットワークの選定や見積もり、要件に応じたリソース構成の計画を問う分野です。（解説は作問時に拡充）' },
    { slug: 'deploy-implement', titleJa: 'クラウドソリューションのデプロイと実装', titleEn: 'Deploying and implementing a cloud solution', weightPercent: 25,
      intro: 'Compute Engine・GKE・Cloud Run・App Engine などへのデプロイと、データ・ネットワークの実装を問う、比重最大の分野です。（解説は作問時に拡充）' },
    { slug: 'operate', titleJa: 'クラウドソリューションの運用', titleEn: 'Ensuring successful operation of a cloud solution', weightPercent: 20,
      intro: 'リソースの監視・ロギング、スケーリング、コスト管理、リソースの管理・保守など、運用フェーズの実務を問う分野です。（解説は作問時に拡充）' },
    { slug: 'access-security', titleJa: 'アクセスとセキュリティの構成', titleEn: 'Configuring access and security', weightPercent: 20,
      intro: 'IAM ロール・サービスアカウント・最小権限の原則など、アクセス制御とセキュリティの構成を問う分野です。（解説は作問時に拡充）' },
  ],
};

export const EXAMS: Exam[] = [GENAI_LEADER, ACE];

export function getExam(slug: string): Exam | undefined {
  return EXAMS.find((e) => e.slug === slug);
}
export function getCategory(examSlug: string, catSlug: string): ExamCategory | undefined {
  return getExam(examSlug)?.categories.find((c) => c.slug === catSlug);
}
```

- [ ] **Step 4: テスト成功を確認** — `npx vitest run src/data/exams.test.ts` → PASS（4 tests）

- [ ] **Step 5: Commit** — `git add -A && git commit -m "feat: add exams.ts as the multi-exam metadata source"`

---

## Task 2: `schema.ts` を examId 対応に改修

**Files:**
- Modify: `src/lib/schema.ts`
- Modify: `src/lib/schema.test.ts`

- [ ] **Step 1: `schema.test.ts` を新シグネチャに更新**（`valid` から `category` を `'fundamentals'` のまま使い、呼び出しを4引数に）

```ts
// validateQuestions の describe を差し替え
describe('validateQuestions', () => {
  const cats = ['fundamentals', 'improve-output'] as const;
  it('throws with the offending id when invalid', () => {
    const bad = [{ ...valid, id: 'bad-1', correct: 99 }];
    expect(() => validateQuestions(bad, 'genai-leader', 'fundamentals', cats)).toThrow(/bad-1/);
  });
  it('throws when category does not match the file', () => {
    expect(() => validateQuestions([valid], 'genai-leader', 'improve-output', cats)).toThrow(/category/i);
  });
  it('throws when category is not in the exam set', () => {
    const q = { ...valid, category: 'unknown-cat' };
    expect(() => validateQuestions([q], 'genai-leader', 'unknown-cat', cats)).toThrow(/exam/i);
  });
  it('injects examId on success', () => {
    const out = validateQuestions([valid], 'genai-leader', 'fundamentals', cats);
    expect(out[0].examId).toBe('genai-leader');
  });
});
```
（`QuestionSchema` の単体テスト群はそのまま。ただし `valid` は `category:'fundamentals'` のままでよい。）

- [ ] **Step 2: 失敗を確認** — `npx vitest run src/lib/schema.test.ts` → FAIL

- [ ] **Step 3: `schema.ts` を改修**

```ts
// 変更点のみ:
// 1) CATEGORY_SLUGS の固定enumを廃止。category を可変文字列に。
export const QuestionSchema = z
  .object({
    id: z.string().min(1),
    category: z.string().min(1),     // ← z.enum(CATEGORY_SLUGS) から変更
    subTopic: z.string().min(1),
    difficulty: z.enum(['easy', 'medium', 'hard']),
    tags: z.array(z.string()).default([]),
    question: z.string().min(1),
    options: z.array(z.string().min(1)).min(2),
    isMultiple: z.boolean(),
    correct: z.union([z.number().int(), z.array(z.number().int()).min(1)]),
    explanation: z.string().min(1),
    reference: z.url(),
    relatedMaterials: z.array(z.object({ label: z.string(), url: z.url() })).default([]),
  })
  .superRefine((q, ctx) => { /* 既存の correct範囲・isMultiple整合チェックをそのまま維持 */ });

export type Question = z.infer<typeof QuestionSchema> & { examId: string };

export function validateQuestions(
  raw: unknown[],
  examId: string,
  expectedCategory: string,
  validCategories: readonly string[],
): Question[] {
  return raw.map((item, index) => {
    const result = QuestionSchema.safeParse(item);
    if (!result.success) {
      const id = (item as { id?: string })?.id ?? `index ${index}`;
      throw new Error(`Invalid question (${id}): ${result.error.issues.map((i) => i.message).join('; ')}`);
    }
    if (result.data.category !== expectedCategory) {
      throw new Error(`Question ${result.data.id} has category "${result.data.category}" but file expects "${expectedCategory}"`);
    }
    if (!validCategories.includes(result.data.category)) {
      throw new Error(`Question ${result.data.id} category "${result.data.category}" is not in exam "${examId}"`);
    }
    return { ...result.data, examId };
  });
}
```
（`CATEGORY_SLUGS` / `CategorySlug` は削除。`quiz.ts` の `filterByCategory(questions, category: CategorySlug)` は `category: string` に型を緩めるだけで動作する。）

- [ ] **Step 4: `quiz.ts` の型を調整** — `src/lib/quiz.ts` の `filterByCategory` 引数型 `CategorySlug` → `string`、不要 import を削除。

- [ ] **Step 5: テスト成功** — `npx vitest run src/lib/schema.test.ts src/lib/quiz.test.ts` → PASS

- [ ] **Step 6: Commit** — `git commit -am "refactor: schema validates examId + per-exam category set"`

---

## Task 3: 問題JSONの移動と集約ローダ改修

**Files:**
- Move: `src/data/questions/*.json` → `src/data/questions/genai-leader/*.json`
- Create: `src/data/questions/ace/{setup-environment,plan-configure,deploy-implement,operate,access-security}.json`（中身は `[]`）
- Modify: `src/data/questions/index.ts`, `src/data/questions/index.test.ts`

- [ ] **Step 1: ディレクトリ移動**（PowerShell）
```
New-Item -ItemType Directory -Force src\data\questions\genai-leader
Move-Item src\data\questions\fundamentals.json,src\data\questions\google-cloud-offerings.json,src\data\questions\improve-output.json,src\data\questions\business-strategy.json src\data\questions\genai-leader\
New-Item -ItemType Directory -Force src\data\questions\ace
```

- [ ] **Step 2: ACE 空ファイル5つを作成**（各ファイル内容は `[]`）：`setup-environment.json` / `plan-configure.json` / `deploy-implement.json` / `operate.json` / `access-security.json`

- [ ] **Step 3: `index.ts` を改修**

```ts
// src/data/questions/index.ts
import { validateQuestions, type Question } from '../../lib/schema';
import { getExam } from '../exams';

import glFund from './genai-leader/fundamentals.json';
import glGco from './genai-leader/google-cloud-offerings.json';
import glImp from './genai-leader/improve-output.json';
import glBiz from './genai-leader/business-strategy.json';
import aceSetup from './ace/setup-environment.json';
import acePlan from './ace/plan-configure.json';
import aceDeploy from './ace/deploy-implement.json';
import aceOps from './ace/operate.json';
import aceSec from './ace/access-security.json';

function build(examSlug: string, catSlug: string, raw: unknown): Question[] {
  const validCats = getExam(examSlug)!.categories.map((c) => c.slug);
  return validateQuestions(raw as unknown[], examSlug, catSlug, validCats);
}

export const QUESTIONS_BY_EXAM: Record<string, Question[]> = {
  'genai-leader': [
    ...build('genai-leader', 'fundamentals', glFund),
    ...build('genai-leader', 'google-cloud-offerings', glGco),
    ...build('genai-leader', 'improve-output', glImp),
    ...build('genai-leader', 'business-strategy', glBiz),
  ],
  ace: [
    ...build('ace', 'setup-environment', aceSetup),
    ...build('ace', 'plan-configure', acePlan),
    ...build('ace', 'deploy-implement', aceDeploy),
    ...build('ace', 'operate', aceOps),
    ...build('ace', 'access-security', aceSec),
  ],
};

export const ALL_QUESTIONS: Question[] = Object.values(QUESTIONS_BY_EXAM).flat();

export function questionsFor(examSlug: string, catSlug: string): Question[] {
  return (QUESTIONS_BY_EXAM[examSlug] ?? []).filter((q) => q.category === catSlug);
}
```

- [ ] **Step 4: `index.test.ts` を改修**

```ts
import { describe, it, expect } from 'vitest';
import { ALL_QUESTIONS, QUESTIONS_BY_EXAM, questionsFor } from './index';

describe('question dataset integrity', () => {
  it('loads without schema errors', () => { expect(ALL_QUESTIONS.length).toBeGreaterThan(0); });
  it('has unique ids across all exams', () => {
    const ids = ALL_QUESTIONS.map((q) => q.id);
    expect(new Set(ids).size).toBe(ids.length);
  });
  it('genai-leader still has 80 questions', () => {
    expect(QUESTIONS_BY_EXAM['genai-leader'].length).toBe(80);
  });
  it('every question carries its examId', () => {
    expect(ALL_QUESTIONS.every((q) => q.examId === 'genai-leader' || q.examId === 'ace')).toBe(true);
  });
  it('questionsFor filters by exam+category', () => {
    expect(questionsFor('genai-leader', 'fundamentals').length).toBe(24);
  });
});
```

- [ ] **Step 5: テスト＆ビルド** — `npm test` と `npm run build` → PASS（ace は0問でも通る）

- [ ] **Step 6: Commit** — `git add -A && git commit -m "refactor: split questions by exam, add empty ACE files"`

---

## Task 4: クイズページを `/[exam]/categories/[slug]/` に移設

**Files:**
- Create: `src/pages/[exam]/categories/[slug].astro`（現 `pages/categories/[slug].astro` を exam 対応に）

- [ ] **Step 1: 実装**（空カテゴリは「準備中」表示で破綻させない）

```astro
---
import BaseLayout from '../../../layouts/BaseLayout.astro';
import Quiz from '../../../components/Quiz.astro';
import RelatedMaterials from '../../../components/RelatedMaterials.astro';
import { EXAMS, getExam, getCategory } from '../../../data/exams';
import { questionsFor } from '../../../data/questions';
import { withBase } from '../../../lib/url';

export function getStaticPaths() {
  return EXAMS.flatMap((exam) =>
    exam.categories.map((cat) => ({ params: { exam: exam.slug, slug: cat.slug }, props: { exam, cat } })),
  );
}
const { exam, cat } = Astro.props as { exam: ReturnType<typeof getExam>; cat: ReturnType<typeof getCategory> };
const e = exam!; const c = cat!;
const questions = questionsFor(e.slug, c.slug);
const title = `${c.titleJa}の問題集`;
const description = `${c.titleJa}（${e.titleJa}）の無料・日本語の練習問題${questions.length}問。即時に正誤と解説を確認できます。`;
const path = `/${e.slug}/categories/${c.slug}/`;
---
<BaseLayout title={`${title}｜${e.shortJa}`} description={description} path={path}>
  <div class="quiz-page-head">
    <h1 class="quiz-page-title">{title}</h1>
    <span class="badge">{e.shortJa}・出題比率 約{c.weightPercent}%</span>
  </div>

  {questions.length > 0
    ? <Quiz questions={questions} />
    : <p class="quiz-empty">この分野の問題は準備中です。近日追加予定です。</p>}

  <section class="quiz-about" aria-labelledby="about-heading">
    <h2 id="about-heading" class="quiz-about__title">この分野について</h2>
    <p>{c.intro}</p>
  </section>

  <RelatedMaterials />

  <p class="quiz-page-foot"><a href={withBase(`/${e.slug}/`)}>← {e.shortJa} トップにもどる</a></p>
</BaseLayout>

<script type="application/ld+json" set:html={JSON.stringify({
  '@context': 'https://schema.org', '@type': 'Quiz', 'name': title,
  'about': { '@type': 'Thing', 'name': e.titleJa },
  'hasPart': questions.map((q) => ({
    '@type': 'Question', 'eduQuestionType': 'Multiple choice', 'text': q.question,
    'acceptedAnswer': { '@type': 'Answer', 'text': (Array.isArray(q.correct) ? q.correct : [q.correct]).map((i) => q.options[i]).join(' / ') },
  })),
})} />
```

- [ ] **Step 2: `.quiz-empty` の最小スタイルを `global.css` に追加**
```css
.quiz-empty { margin-block: var(--space-8); padding: var(--space-6); text-align: center; color: var(--color-text-muted); background: var(--bg-surface); border: var(--border-width) solid var(--color-border); border-radius: var(--radius-lg); }
```

- [ ] **Step 3: ビルド** — `npm run build` → `/genai-leader/categories/...`（4）と `/ace/categories/...`（5・準備中）が生成。

- [ ] **Step 4: Commit** — `git add -A && git commit -m "feat: add /[exam]/categories/[slug] quiz page"`

---

## Task 5: 試験トップ `/[exam]/`

**Files:**
- Create: `src/pages/[exam]/index.astro`

- [ ] **Step 1: 実装**（現 `index.astro` のカテゴリグリッド＋進捗を exam 対応に）

```astro
---
import BaseLayout from '../../layouts/BaseLayout.astro';
import { EXAMS, getExam } from '../../data/exams';
import { questionsFor } from '../../data/questions';
import { withBase } from '../../lib/url';

export function getStaticPaths() {
  return EXAMS.map((exam) => ({ params: { exam: exam.slug }, props: { exam } }));
}
const { exam } = Astro.props as { exam: ReturnType<typeof getExam> };
const e = exam!;
const total = e.categories.reduce((s, c) => s + questionsFor(e.slug, c.slug).length, 0);
const title = `${e.titleJa} 無料問題集（日本語）`;
---
<BaseLayout title={`${title}｜${e.shortJa}`} description={e.description} path={`/${e.slug}/`}>
  <div class="page-hero">
    <h1 class="page-title">{e.shortJa}<br />無料 練習問題集</h1>
    <p class="page-description">{e.description}（全{total}問・登録不要）</p>
  </div>
  <section aria-labelledby="category-heading">
    <h2 id="category-heading" style="font-size:var(--text-xl);font-weight:var(--weight-bold);margin-block:var(--space-8) var(--space-4);color:var(--color-grey-900);">カテゴリから学習を始める</h2>
    <ul class="category-list">
      {e.categories.map((c) => (
        <li>
          <a href={withBase(`/${e.slug}/categories/${c.slug}/`)} style="text-decoration:none;display:block;">
            <div class="card card--hover">
              <p class="cat-title">{c.titleJa}</p>
              <p class="cat-meta">全{questionsFor(e.slug, c.slug).length}問・出題比率{c.weightPercent}%</p>
              <p class="cat-desc">{c.intro}</p>
              <div class="cat-progress"><div class="cat-progress__bar" data-bar-slug={c.slug} style="--pct: 0%"></div></div>
              <p class="cat-meta" data-category-progress={c.slug} style="margin-block-start:var(--space-2);margin-block-end:0;">未着手</p>
            </div>
          </a>
        </li>
      ))}
    </ul>
  </section>
</BaseLayout>

<script define:vars={{ examSlug: e.slug }}>
  import { loadProgress } from '../../lib/storage';
  import { categoryStats } from '../../lib/progress';
  import { questionsFor } from '../../data/questions';
  const p = loadProgress();
  document.querySelectorAll('[data-category-progress]').forEach((el) => {
    const slug = el.dataset.categoryProgress;
    const ids = questionsFor(examSlug, slug).map((q) => q.id);
    const s = categoryStats(p, ids);
    el.textContent = s.answered > 0 ? `進捗 ${s.answered}/${s.total}・正答率 ${Math.round(s.rate * 100)}%` : '未着手';
    const bar = document.querySelector(`[data-bar-slug="${slug}"]`);
    if (bar && s.total > 0) bar.style.setProperty('--pct', `${Math.round((s.answered / s.total) * 100)}%`);
  });
</script>
```
> 注: client `<script>` で `examSlug` を使うため `define:vars` で受け渡す。`questionsFor` はビルド時にバンドルされる純関数なのでクライアントでも利用可。

- [ ] **Step 2: ビルド** — `/genai-leader/` と `/ace/` が生成。

- [ ] **Step 3: Commit** — `git add -A && git commit -m "feat: add /[exam] exam home page"`

---

## Task 6: 試験選択トップ `/`

**Files:**
- Modify: `src/pages/index.astro`（カテゴリ一覧 → 試験選択に）

- [ ] **Step 1: 実装**

```astro
---
import BaseLayout from '../layouts/BaseLayout.astro';
import { EXAMS } from '../data/exams';
import { ALL_QUESTIONS } from '../data/questions';
import { withBase } from '../lib/url';
const total = ALL_QUESTIONS.length;
const countFor = (slug: string) => ALL_QUESTIONS.filter((q) => q.examId === slug).length;
const title = 'Google Cloud 認定 演習場 — 無料問題集（日本語）';
const description = `Google Cloud 認定試験の無料・日本語練習問題集。現在 ${EXAMS.length} 試験・計${total}問を即時フィードバックで演習できます。`;
---
<BaseLayout title={title} description={description} path="/">
  <div class="page-hero">
    <h1 class="page-title">Google Cloud 認定<br />無料 演習場</h1>
    <p class="page-description">{description}</p>
  </div>
  <section aria-labelledby="exam-heading">
    <h2 id="exam-heading" style="font-size:var(--text-xl);font-weight:var(--weight-bold);margin-block:var(--space-8) var(--space-4);color:var(--color-grey-900);">試験を選ぶ</h2>
    <ul class="category-list">
      {EXAMS.map((e) => (
        <li>
          <a href={withBase(`/${e.slug}/`)} style="text-decoration:none;display:block;">
            <div class="card card--hover">
              <p class="cat-title">{e.titleJa}</p>
              <p class="cat-meta">{e.shortJa}・全{countFor(e.slug)}問</p>
              <p class="cat-desc">{e.tagline}</p>
            </div>
          </a>
        </li>
      ))}
    </ul>
  </section>
</BaseLayout>
```

- [ ] **Step 2: ビルド＆preview** — `npm run build` → `/` が試験2カード。

- [ ] **Step 3: Commit** — `git add -A && git commit -m "feat: turn top page into exam selector"`

---

## Task 7: 旧 `/categories/[slug]/` をリダイレクト退避

**Files:**
- Modify: `src/pages/categories/[slug].astro`（中身をリダイレクトに）

- [ ] **Step 1: 実装**（genai-leader の4カテゴリのみ生成し、新URLへメタリフレッシュ＋canonical）

```astro
---
import { getExam } from '../../data/exams';
import { withBase } from '../../lib/url';
export function getStaticPaths() {
  return getExam('genai-leader')!.categories.map((c) => ({ params: { slug: c.slug }, props: { slug: c.slug } }));
}
const { slug } = Astro.props as { slug: string };
const dest = withBase(`/genai-leader/categories/${slug}/`);
const canonical = new URL(dest, Astro.site).toString();
---
<html lang="ja">
  <head>
    <meta charset="utf-8" />
    <meta http-equiv="refresh" content={`0; url=${dest}`} />
    <link rel="canonical" href={canonical} />
    <meta name="robots" content="noindex, follow" />
    <title>移動しました</title>
  </head>
  <body>
    <p>このページは <a href={dest}>{canonical}</a> に移動しました。</p>
  </body>
</html>
```

- [ ] **Step 2: ビルド** — 旧4URLが新URLへ転送。

- [ ] **Step 3: Commit** — `git add -A && git commit -m "feat: redirect legacy /categories to /genai-leader/categories"`

---

## Task 8: ブランド・共通レイアウト調整

**Files:**
- Modify: `src/layouts/BaseLayout.astro`

- [ ] **Step 1: 変更**
  - `const siteName = 'Google Cloud 認定 演習場';`
  - ロゴ文言：`GenAI Leader <span>演習場</span>` → `Google Cloud <span>認定 演習場</span>`
  - ヘッダーナビ「カテゴリ」の `href` を `withBase('/categories/')` → `withBase('/')`（試験選択トップ）。文言は「試験一覧」。

- [ ] **Step 2: ビルド＆preview 全導線確認**
  Run: `npm run build` → `npm run preview`
  確認: `/`（試験2枚）→ `/genai-leader/`（4カテゴリ・80問・進捗）→ クイズ即時FB／`/ace/`（5カテゴリ・準備中）／旧 `/categories/fundamentals/` が新URLへ転送。

- [ ] **Step 3: 全テスト＆ビルド** — `npm test`（exams/schema/quiz/progress/index 全緑）＋ `npm run build`。

- [ ] **Step 4: Commit & デプロイ** — `git add -A && git commit -m "feat: rebrand to Google Cloud 認定 演習場 (multi-exam)" && git push` → `gh run watch` で success 確認。

---

## Task 9（次フェーズ）: ACE 作問（サブエージェント並列）

**Files:** `src/data/questions/ace/*.json`（5ファイルに追記）, `index.test.ts`（件数）

- [ ] `docs/2026-06-05-問題追加-作問ガイド.md` を **ACE 用に展開**（カテゴリ5・id命名・難易度配分・reference公式ドメインを決定）。
- [ ] `superpowers:dispatching-parallel-agents` で **5カテゴリ＝5サブエージェント** 起動（各 general-purpose、reference を WebFetch 検証）。
- [ ] メインがレビュー → JSON追記 → `index.test.ts` に ACE 件数を追加 → `npm test` / `npm run build` → preview → コミット → デプロイ。

> 作問の品質基準・スキーマ・reference検証は GenAI Leader の80問化と同一手順。

---

## Self-Review（計画著者チェック）

- **スペック網羅:** §2 exam概念→T1, §3.2 schema→T2, §3 データ構造→T3, §4 ルーティング→T4/T5, §3試験選択→T6, §4旧URL→T7, §7ブランド→T8, §6 storage→変更なし（設計反映済）, ACEカテゴリ→T1, ACE作問→T9。網羅。
- **プレースホルダ:** ACE intro は「作問時に拡充」と明示（枠は機能する実テキスト入り）。コードは全て実コード。
- **型整合:** `Exam`/`ExamCategory`（exams.ts）, `Question & {examId}`・`validateQuestions(raw,examId,cat,validCats)`（schema.ts）, `questionsFor`/`QUESTIONS_BY_EXAM`（index.ts）は定義タスクと利用タスクで名称一致を確認。
